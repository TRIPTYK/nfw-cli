The email and password for your super admin :

email : <%- login %>
password : <%- password %>

You can login with your account using this route :

===============================================
POST /api/v1/auth/login
ACCEPT : application/vnd.api+json
CONTENT-TYPE : application/vnd.api+json
JSON :
{
    "email":"<%- login %>",
    "password":"<%- password %>"
}
===============================================

You will receive a response with an accessToken and an ID

You can then modify your user using the ID from the response

===============================================
PATCH /api/v1/users/ID
ACCEPT : application/vnd.api+json
CONTENT-TYPE : application/vnd.api+json
BEARER : accessToken
JSON :
{
    "data" : {
        "attributes" : {
        "password" : "newPassword",
        "username" : "newUsername"
        }
    }
}
===============================================

Don't forget to delete this file !