import * as global from "./global";

describe("AddColumnCommand", function () {
	before(global.createSimpleProject);
	//Your test here :)
	const projet = global.projectName;
	after(global.cleanProject);
});
