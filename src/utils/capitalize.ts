
/**
 * Returns the capitalized input.
 * @param input Any word idk
 * @returns Capitalized version of word
 */
export function capitalize(input: string) {
    return `${input[0].toUpperCase()}${input.slice(1)}`;
}